name: L1 Rules Smoke

on:
  push:
  pull_request:


concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true


jobs:
  l1-smoke:
    runs-on: ubuntu-latest
    env:
      L0_VALIDATORS: pilot
    steps:
      - uses: actions/checkout@v4

      - name: Set up OCaml and OPAM
        uses: ./.github/actions/setup-ocaml
        with:
          ocaml-compiler: 4.14.1
          opam-depext: false
      - name: Install dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y jq
          opam repo add default https://opam.ocaml.org || true
          opam update -y || true
          opam install -y . --deps-only --with-test

      - name: Build service and REST
        run: |
          eval "$(opam env)"
          dune build latex-parse/src/main_service.exe latex-parse/src/rest_api_server.exe

      - name: Run service and REST
        run: |
          eval "$(opam env)"
          rm -f /tmp/l0_lex_svc.sock
          L0_VALIDATORS=pilot ./_build/default/latex-parse/src/main_service.exe &
          sleep 0.5
          L0_VALIDATORS=pilot ./_build/default/latex-parse/src/rest_api_server.exe -p 8080 &
          sleep 0.5

      - name: L1 smokes
        run: |
          bash scripts/smoke_rules_l1.sh

      - name: Teardown
        if: always()
        run: |
          pkill -f rest_api_server || true
          pkill -f main_service || true
