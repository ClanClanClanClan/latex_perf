# L2 Parser (Phase 3)

This directory is reserved for the L2 parser layer when it moves from
`latex-parse/src/parser_l2.ml` into the formally-verified core architecture.

The active L2 parser implementation lives in `latex-parse/src/parser_l2.ml`.

**Status:** Stub — implementation planned for Weeks 35+ (Phase 3).
