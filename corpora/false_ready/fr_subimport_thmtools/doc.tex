\documentclass{article}
\usepackage{import}
\subimport{tex}{header.tex}
\begin{document}
Never reached: the preamble reached only through \subimport declares a
shared-counter theorem after loading thmtools.
\end{document}
