# PROJECT STATE — the single source of truth

**This document is the ONE place that states where the project actually is.**
If another document disagrees with this one, this one wins and the other is a bug.

| document | role |
|---|---|
| **`PROJECT_STATE.md`** (this file) | **where we ARE** — measured position, open ledger, corrections |
| `ROADMAP.md` | where we INTEND to go — strategy, tracks, principles |
| `AUDIT_R7_FIX_PLAN.md` | one programme's execution record (round-7). Historical |
| `corpora/*/manifest.json`, `corpora/real_roots/results.json` | the artefacts that OWN the numbers |

**Why this file exists.** Before it, the most accurate picture of this project
lived in three places a future reader could not reach: an agent's private memory,
ephemeral `/tmp` workflow outputs, and PR descriptions. The repo itself carried a
ROADMAP whose banner, false-READY count, over-rejection claim and
version-of-record were each factually false while `check_roadmap_facts.py`
printed `Roadmap facts check passed`. Measured on 2026-08-22, **no in-repo prose
carried the real-paper measurement at all**, and the strings `declared_texlive`,
`rescue-alone` and the `corpora/unicode` finding appeared in **zero** files.

**The rules that keep it true.**

1. Numbers in the generated block below come from the artefact that owns them.
   `check_project_state.py` regenerates and diffs. **Never hand-edit that block.**
2. Every open item has a stable `OPEN-nnn` id. Cite it in the PR that closes it.
   Ids are never reused.
3. Every claim carries provenance: a file:line, a command, or `UNVERIFIED`.
4. When a belief turns out to be wrong, it goes in §4 — the corrections log.
   That section is the point of this document, not an appendix to it.

---

## 1. Measured position

<!-- BEGIN GENERATED: measured-position -->

> Generated by `scripts/tools/gen_project_state.py`. **Do not hand-edit.**
> `check_project_state.py` regenerates this block and fails on any diff,
> so a number here can only be wrong if its source artefact is wrong.

### The three quantities that must never be conflated

Each grades a different corpus. They are disjoint. Conflating them is the
single most repeated error in this project's history.

| | corpus | value | what moves it |
|---|---|---|---|
| **(a)** differential allowlist | `corpora/compile_check`, 65 hand-authored docs | **8** | S6/S7-style detectors |
| **(b)** fixture baseline | `corpora/false_ready`, 23 fixtures | **8** (7 strong-fatal, 1 error-halt) | R7 fix ranks |
| **(c)** **real papers** | `corpora/real_roots`, 200 arXiv trees (frame 2719) | **11 / 199 = 5.5%** | *the only one that is the North Star* |

### Real-paper position

Oracle `pdfTeX 3.141592653-2.6-1.40.29`, TeX Live 2026, protocol `-interaction=nonstopmode -halt-on-error -no-shell-escape`.

| cell | n |
|---|---|
| true-READY | 107 |
| true-NOT-READY | 6 |
| FALSE-READY | 11 |
| false-NOT-READY | 75 |
| ungraded-infra | 1 |
| ungraded-timeout | 0 |

- **Correct verdicts: 113/199 = 56.8%**
- **Over-rejection: 75/199 = 37.7%** — i.e. **92.6% of every NOT-READY verdict issued on a real paper is wrong**
- **False-READY: 11/199 = 5.5%**, against a definition requiring zero

### Fixer residual (auto-fix channel)

| property | rows |
|---|---|
| `breaks_compile` | 0 |
| `degrades_verdict` | 0 |
| `not_idempotent` | 3 |
| `manufactured_false_ready` | 0 |

Total recorded rows: **3**. pdflatex-graded: `True`.

### Infrastructure

- Required CI contexts: **11** — build, l1-smoke, perf-ci, proof-ci, rest-smoke, smoke-cli, spec-drift, tex-oracle, unicode-smoke, unit-tests, xxh-selfcheck
- Version of record: **v27.1.62** (`project_facts.yaml` release_date 2026-07-27)
- Release debt: run `git describe --tags`. A distance greater than 0 means merged work is unreleased, and **no gate compares any version marker to a tag** (OPEN-013) — so this is the one number here you must check by hand.

<!-- END GENERATED: measured-position -->

---

## 2. Where we are, in one paragraph

The verification substrate is now trustworthy and the fixer is safe. The gates
bind (required contexts include `build` and `tex-oracle`, and real pdflatex runs
in CI against a digest-pinned image). The fixer's measured damage is zero on the
round-trip corpus, and both of its byte channels are screened. What changed the
picture is that the tool was graded against real documents for the first time:
the accuracy is much worse than the fixture corpora suggested, and **the dominant
failure mode is over-rejection, not the cardinal bug**. The R7 programme is
complete as designed and superseded as strategy — it moved fixture baseline (b)
from 21 to 7 while the published banner moved 35 → 34, because (a), (b) and (c)
are disjoint corpora. The next phase is accuracy work, and most of it is not in
the ROADMAP.

---

## 3. Open ledger

Class key: **SOUND** false-READY · **OVERREJ** over-rejection · **INSTR**
measurement · **HONEST** claims vs reality · **GATE** CI · **TRACK** untouched.

Size: S ≤ a day · M ≤ a week · L multi-week · XL multi-month.

| id | class | item | evidence | size |
|---|---|---|---|---|
| OPEN-001 | SOUND | **`corpora/unicode` was never graded against pdflatex.** 6 of its 14 fixtures are live false-READYs (U+200D, U+FFFD, U+2212, U+200B, U+0301, U+22EF: CLI READY, pdflatex rc 1, no PDF). The true committed residual is **13, not 7**. Also `CHAR-021` is a second vacuous T5 rule (CLI NOT-READY, pdflatex compiles). | verified 2026-08-22 by compiling all 14 | S |
| OPEN-002 | SOUND | **`\c@<env>` theorem-counter collision** — 8 of the 11 real-paper false-READYs. `thm-restate` patches `\newtheorem`; a shared-counter `\newtheorem{lemma}[theorem]` then collides. Fixtures exist (`fr_thmtools_redeclare` strong-fatal, `fr_thmrestate_counter` error-halt); **no detector does.** | `corpora/real_roots/results.json` | M |
| OPEN-003 | SOUND | **natbib author-year vs numeric `.bst`** — 3 of the 11. Needs a `.bbl` to reproduce; no fixture yet. | same | M |
| OPEN-004 | SOUND | **SEC1: `\input` escapes the project root.** A root containing `\input{../outside/secret}` where the target lies OUTSIDE the project dir returns **READY rc 0**. ⚠ The originally-reported second half is **REFUTED**: absolute paths are not naively concatenated — `\input{/etc/hostname}` correctly returns NOT-READY, as does a missing target (control). So the defect is relative-path escape only. | **VERIFIED 2026-08-22** by reproduction | S |
| OPEN-005 | INSTR | **`declared_texlive` is null on all 200 rows** of `corpora/real_roots/manifest.json`. The runner reads the key but never resolves it, so the TL2023-vs-TL2026 drift split the README mandates is unfollowable — and that question gates 8 of the 11 false-READYs. | verified 2026-08-22 | S |
| OPEN-006 | OVERREJ | **T2 comment-blindness** — 16 rescue-alone documents. ⚠ **12 compile, 4 do NOT.** T2 is the only thing catching those 4 real failures, so a naive relaxation converts them into false-READYs. Must be a precision fix (blank comments before scanning), never a weakening. | verified 2026-08-22 from results.json | M |
| OPEN-007 | OVERREJ | **T3 feature-detection comment-blindness** — 13 rescue-alone, all 13 compile. Pure gain. | same | M |
| OPEN-008 | OVERREJ | **T4 duplicate-`\label` polarity** — 9 rescue-alone, all 9 compile. pdflatex only warns and exits 0. | same | S |
| OPEN-009 | OVERREJ | **REAL, but the obvious fix MANUFACTURES A FALSE-READY — re-sized S → L.** The gap is real: `compile_gate_checks.ml`'s opt-skip loop accepts `' '`/`\t`/`\n`/`\r`/`[` but not `*`, so in math `\ref*{sec:x_a_b}` is NOT-READY while `\ref{...}` is READY. ⚠ **Do NOT just add `*`.** That makes the detector SKIP the span, and a document whose `\ref` is redefined in an `\input` CHILD (`\renewcommand{\ref}[1]{\textbf{#1}}`) then typesets the key: today NOT-READY with pdflatex rc 1 `! Double subscript.` and no PDF — **after the patch, READY**. Measured yield of the cheap patch is **zero**: 0 of 200 sampled papers and 0 of 2,961 in the frame change verdict. A safe fix must resolve `\ref` redefinition across the INCLUDE CLOSURE (and `\let`), not scan the root — hence L. Net of the cheap version: strictly negative. | **VERIFIED 2026-08-22** — both the gap and the refutation reproduced end-to-end | L |
| OPEN-010 | OVERREJ | **DELIM-001 / DELIM-003 residual precision** — 2 and 1 rescue-alone, all compile. Unlike DELIM-007 these ARE real error classes, so the fix is precision, not demotion. | verified 2026-08-22 | M |
| OPEN-011 | INSTR | **`corpora/real_roots` is ungated.** No workflow reads it; an ungated measurement decays exactly as the 65-doc banner did. Gate false-READY *set membership*, not a numeric over-rejection bound. | no workflow references it | M |
| OPEN-012 | INSTR | **Extend 200 → 400** and publish the new-class discovery rate. Every instrument so far found more (round-7 +30, Aug audit +6, this corpus +2). It is the only real test of "is the residual enumerable". | — | L |
| OPEN-013 | GATE | **No gate compares any version marker to a git tag.** 33 commits past `v27.1.62` with 11 green required contexts. | `git describe` vs `dune-project` | S |
| OPEN-014 | HONEST | **`per_rule_soundness_count: 643`** is `rules.total_non_reserved` copied verbatim; the 803 generated theorems are 803/803 identical one-line instantiations of a `flat_map` triviality; `theorem_count_reported: 1543` vs 1456 line-anchored. | `generate_project_facts.py:104` | M |
| OPEN-015 | HONEST | **The shipped verdict string cites Coq** — `MODEL-READY (Coq project_wf_dec_sound => pdflatex_compile_safe)` — and is demonstrably wrong on 5.5% of real papers. `compile_gate_checks.ml` (1,011 lines), which produces the verdict, has zero proof-derived references. | verified | M |
| OPEN-016 | HONEST | **ROADMAP internal contradiction**, narrowed by line-by-line check. CONFIRMED: `:58` banner says **35/65** while `:140` of the same document says **34/20/8/3** and `COMPILATION_GUARANTEE.md:243` agrees with `:140` — so the banner contradicts its own document. CONFIRMED: `:159` still lists S0 as NOT STARTED though `corpora/real_roots` shipped half of it. ⚠ **REFUTED**: there is no second live matrix (`:140` states the correct tuple *and* explains the superseded one), and `:84`'s "0 over-rejection" is a REQUIREMENT ON NEW GATES plus honest history, not a claim about the tool — though the tool would now fail that requirement at 37.7%. | **VERIFIED 2026-08-22** line-by-line | S |
| OPEN-017 | GATE | **`check_gates_meta.py` never inspects `returncode`** and covers 16 of ~29 gate scripts; six gates run in no CI path (`check_ast_parity` among them, which `README.md:85` cites as evidence). | reported by audit, partially verified | M |
| OPEN-018 | SOUND | **OR-1 / DWR-1**: `validators_common.ml:623-638` takes the byte after `\lstinline` as the delimiter, guarded only by "not a letter", so an optional argument makes `[` the delimiter. THREE distinct defects, and the trigger is narrower than recorded. **(a) CORRUPTION**, needs a `[` inside the option VALUE: `\lstinline[caption={[x]}]\|a--b\|` → the fixer rewrites the verbatim to `a–b`. **(b) SILENT OVER-REACH**, the common case: with no later `[` the range runs to EOF, so every fix in the REST of the document is withheld — verified, `prose -- here` after an `\lstinline[language=C]` is never fixed. **(c) BLANK-SKIP**: the delimiter is read at a fixed offset, but TeX absorbs a space after a control word — `X \verb |ab| Y` compiles (rc 0) with `|` as the delimiter, while the scanner takes the SPACE. Any fix must skip blanks in ONE shared helper used by both the scanner and `verb_broken_eol_fatal`, or the re-key re-implements the same wrong rule. Plain `\lstinline\|a--b\|` is correct. Named blocker on any fixer-soundness claim and prerequisite for OPEN-006/007. | **VERIFIED 2026-08-22** by reproduction | M |
| OPEN-019 | TRACK | **Track P (policy)** — `editorial_policy.ml` accepts exactly `profile`, `disable`, `enable`, `severity`, `waive`. No `require`, no `forbid`, no `venue`. A channel that can only SUPPRESS findings cannot express a house style, so segment B's flagship is not started. | **VERIFIED 2026-08-22** — accepted-key grep | L |
| OPEN-020 | TRACK | **R3a byte offsets** — `validators.mli:16-27` defines `result = { id; severity; message; count; fix; candidate_fixes }`. **There is no span or offset field**, so R3a's stated source does not exist and it is a type change through ~660 rule bodies, not the S-sized item the plan assumes. | **VERIFIED 2026-08-22** | L |
| OPEN-021 | TRACK | **Track R (real-time)** — 3 of 18 items shipped. Cold path 512–702 ms @300 KB vs a 3 ms SLO; the ratchet baseline predates #525 and is ~2.8–3.4× loose; `bench_readiness_kernel.ml` reuses one physical string across reps, keeping the very cache #525 added hot. Re-record only AFTER fixing the bench. | `corpora/perf/keystroke_budget.json` | L |
| OPEN-022 | INSTR | **`corpora.lock` is 100% placeholder** — every entry points at `example.com` with `sha256: "placeholder-update-after-first-download"`. | verified by audit | S |

**Closed recently** (cite the PR): C1 dead required check → #533 · C2 gates non-binding → #532/#534 · C3 fixer destroys files → #519/#520/#529/#531/#536 · C4 candidate channel unguarded → #540 · DELIM boundary bug → #535 · R-CHANNEL naming hole + DELIM-007 → this branch.

---

## 4. Corrections log — what we got wrong

The most valuable section. Each entry is a belief that was acted on and turned
out to be false, plus how it was caught, so the same mistake is not repeated.

| # | We believed | Actually | How it surfaced |
|---|---|---|---|
| C-1 | Driving the false-READY **fixture** baseline down was progress toward the North Star. | The corpora are **disjoint**. 21 → 7 across ~15 PRs moved the published banner **35 → 34**. | Running the first differential against real papers (#538). |
| C-2 | The residual false-READY set was **small and enumerable**. | 11/199 = 5.5% on real papers, and every instrument pointed at the problem has found more. | Same. |
| C-3 | The cardinal bug (false-READY) was the dominant failure mode. | **Over-rejection is far larger**: 37.7%, i.e. 92.6% of every NOT-READY verdict on a real paper is wrong. | Same. |
| C-4 | The T5 rule engine was catching compile failures. | On 200 real papers the compile-blocking set fired on 25 documents, **all 25 compile**, and caught **none** of the 6 real failures (T2 caught 5, T0 caught 1). ⚠ The corpus is papers arXiv *built*, so this shows miscalibration for the real population, **not** vacuity on broken input. | Recomputing from `results.json`. |
| C-5 | A verdict flip from NOT-READY to READY is a win. | Only if the oracle agrees. Of 22 flips from the DELIM fix, **20 compiled and 2 did not** — the bug had been masking two real false-READYs. | Recompiling all 22 before claiming the result. |
| C-6 | A fixture reproducing the right *error message* is the right fixture. | `fr_thmtools_redeclare` is **strong-fatal**; the wild-type shape is **error-halt**. A detector validated only on the synthetic one would miss every real paper. | Measuring both pinned protocols for both fixtures. |
| C-7 | Adding a corpus of lint fixtures was safe. | `corpora/unicode` (#533) was never graded against pdflatex; **6 of 14 are live false-READYs**. | Auditing my own work after the position assessment. |
| C-8 | "Carried" reason counts show which fix rescues most documents. | They rescue **nothing** — `T5` co-occurs with every T5 rule id by construction. Only **rescue-alone** (a document's *only* blocking reason) is actionable. | Recomputing; the first ordering was wrong. |
| C-9 | A green local checklist meant the change was verified. | Two runs reported "clean" that had been **killed before writing output**. Background jobs now write a `SENTINEL_DONE` marker. | A CI `format` failure on #535 that the local check had "passed". |
| C-10 | `check_roadmap_facts.py` passing meant the ROADMAP was true. | It asserts only the numbers it knows, via `re.search`, so of two contradictory matrices only the first is checked. It passed while ≥4 statements were false. | Reading the gate. |
| C-11 | Findings reported by an audit agent could be recorded and planned against as-is. | Of **six** such items, **two were wrong**. There is no second live confusion matrix in the ROADMAP (`:140` states the correct tuple and explains the superseded one), and `:84`'s "0 over-rejection" is a *requirement on new gates*, not a claim about the tool. A third, OPEN-004, was half wrong: relative `\input` does escape the project root, but absolute paths are correctly rejected. | Reproducing all six before planning. Items now carry VERIFIED-with-evidence or say UNVERIFIED. |
| C-12 | The `\lstinline` scanner defect was "a misplaced range exposing verbatim". | Narrower and doubled. Corruption needs a `[` inside the option VALUE (`\lstinline[caption={[x]}]\|a--b\|` → `a–b`). The common case is the opposite failure: the range runs to **EOF**, silently withholding every fix in the rest of the document. The second half was recorded nowhere. | Reproducing it instead of trusting the summary. |
| C-13 | A generated block should carry every fact worth knowing, including release debt. | `git describe` changes on **every commit**, so the regenerate-and-diff gate fired on the very next one. A gate that fires on every PR trains people to regenerate without reading, which is worse than no gate. Volatile facts belong in prose as an instruction, not in a diffed block. | The gate failing on my own next commit. |
| C-14 | OPEN-009's `\ref*` gap was "a one-character fix", cheap and safe. | **Strictly negative as scoped.** Adding `*` to the opt-skip makes the detector skip the span, and a document whose `\ref` is redefined in an `\input` child goes from correctly NOT-READY (pdflatex rc 1, no PDF) to READY. Measured yield of the "fix": **0 of 200** and **0 of 2,961**. So it trades a manufactured false-READY for nothing. | An adversarial agent tasked with REFUTING each "no false-READY risk" claim, then reproduced by hand. The ledger had been instructing the patch. |
| C-15 | The `\verb` scanner's only defect was the optional-argument delimiter. | There is a third: the delimiter is read at a FIXED offset, but TeX absorbs a space after a control word. `X \verb |ab| Y` compiles with `|` as delimiter while the scanner takes the space. A "fix" that re-implements the same offset rule in the re-key inherits the bug. | Same hunt. |

---

## 5. Process invariants

Earned, each at a cost. Violating one is how the entries in §4 happened.

1. **Grade every corpus against the oracle, once, when you add it.** OPEN-001 exists because a corpus was added to test lint firing and never compiled.
2. **A verdict flip is not a win until pdflatex agrees.** (C-5)
3. **Verify a gate in BOTH directions** — clean pass *and* injected failure, naming the expected signal.
4. **Never trust a background run without a completion sentinel.** (C-9)
5. **Never set a latency threshold on this machine.** Load has been observed at 158–230. Interleaved A/B at load ≤ 8 only, and CI and this laptop compile different C tokenizers.
6. **Quote (c), or quote nothing.** (a) and (b) grade toy corpora.
7. **Edit `.github/required-status-checks.json`, never the branch-protection API** — `branch-protection.yml` PUTs the file on every push to main.
8. **Verify PR state via `statusCheckRollup`**, never by reading the check list.
9. **Order a release: CHANGELOG + ROADMAP prose first, `release.sh` second.**

---

## 6. Provenance

The generated block is regenerated by `scripts/tools/gen_project_state.py` and
verified by `scripts/tools/check_project_state.py`, which runs in the **required**
`spec-drift` job. The hand-written sections are the author's responsibility; every
claim in them is either marked verified with its evidence, or marked UNVERIFIED.
