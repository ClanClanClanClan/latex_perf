% Test corpus sample for validator testing
\documentclass{article}

% Test cases for our real validators
\begin{document}

% DELIM-001: Test unmatched braces
\section{Delimiter Tests}
This should work: {balanced braces}.
This will fail: {unmatched brace

% DELIM-003: Test left/right matching  
Math with delimiters: $\left( \frac{a}{b} \right)$
This will fail: $\left( \frac{a}{b}$ % Missing \right

% DELIM-007: Test angle brackets
Quantum state: $\langle \psi | \phi \rangle$
This will fail: $\langle \psi | \phi$ % Missing \rangle

% MATH-009: Test bare function names
Proper math: $\sin(x) + \cos(y)$
This will fail: $sin(x) + cos(y)$ % Bare function names

% MATH-010: Test division symbol
Proper division: $a/b$ or $\frac{a}{b}$
This will fail: $a ÷ b$ % Division symbol

% REF-001: Test undefined references
\label{eq:defined}
This works: see equation~\ref{eq:defined}
This will fail: see equation~\ref{eq:undefined} % Undefined label

% REF-002: Test duplicate labels
\label{eq:first}
\label{eq:first} % This will fail - duplicate label

% REF-003: Test label format
\label{eq:good_label} % This works
\label{eq:bad label} % This will fail - space in label

% SCRIPT-001: Test subscripts
Good subscript: $x_i$
Bad subscript: $x_max$ % Multi-character without braces

% CMD-001: Test unused commands
\newcommand{\usedcmd}{used}
\newcommand{\unusedcmd}{unused} % This will fail - never used

Using the command: \usedcmd

\end{document}